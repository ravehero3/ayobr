<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Settings Window</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            height: 100vh;
            overflow: hidden;
            position: relative;
        }

        /* Animated blurry background */
        .background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, #0f0f23, #1a1a2e, #16213e, #0f0f23);
            background-size: 400% 400%;
            animation: gradientShift 8s ease infinite;
            filter: blur(2px);
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Settings window container */
        .settings-window {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 480px;
            height: 640px;
            background: rgba(8, 8, 12, 0.85);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            border: 2px solid rgba(255, 255, 255, 0.08);
            box-shadow: 
                0 32px 64px rgba(0, 0, 0, 0.6),
                0 8px 16px rgba(0, 0, 0, 0.4),
                inset 0 1px 0 rgba(255, 255, 255, 0.05);
            padding: 32px;
            display: flex;
            flex-direction: column;
            gap: 24px;
        }

        .settings-title {
            color: #e0e0e0;
            font-size: 20px;
            font-weight: 600;
            text-align: center;
            margin-bottom: 8px;
        }

        /* Logo upload area */
        .logo-upload {
            height: 120px;
            background: rgba(15, 15, 25, 0.6);
            border: 2px dashed rgba(255, 255, 255, 0.15);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .logo-upload:hover {
            border-color: rgba(59, 130, 246, 0.4);
            background: rgba(59, 130, 246, 0.05);
        }

        .logo-upload-text {
            color: rgba(255, 255, 255, 0.6);
            font-size: 16px;
            font-weight: 500;
        }

        /* Background selection section */
        .section-title {
            color: #e0e0e0;
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 12px;
            opacity: 0.9;
        }

        .background-options {
            display: flex;
            gap: 16px;
            justify-content: space-between;
        }

        .bg-option {
            flex: 1;
            height: 80px;
            border-radius: 12px;
            cursor: pointer;
            position: relative;
            transition: all 0.3s ease;
            border: 2px solid rgba(255, 255, 255, 0.1);
            overflow: hidden;
        }

        .bg-option.selected {
            border-color: rgba(59, 130, 246, 0.8);
            box-shadow: 
                0 0 20px rgba(59, 130, 246, 0.3),
                inset 0 0 20px rgba(59, 130, 246, 0.1);
        }

        .bg-white {
            background: linear-gradient(135deg, #ffffff, #f8f8f8);
        }

        .bg-black {
            background: linear-gradient(135deg, #1a1a1a, #000000);
        }

        .bg-custom {
            background: linear-gradient(135deg, rgba(30, 58, 138, 0.4), rgba(17, 24, 39, 0.6));
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .bg-custom-text {
            color: #e0e0e0;
            font-size: 12px;
            font-weight: 500;
        }

        /* Resolution options */
        .resolution-options {
            display: flex;
            gap: 16px;
        }

        .res-option {
            flex: 1;
            padding: 20px;
            background: rgba(15, 15, 25, 0.6);
            border: 2px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-align: center;
        }

        .res-option.selected {
            border-color: rgba(59, 130, 246, 0.8);
            background: rgba(59, 130, 246, 0.05);
            box-shadow: 
                0 0 20px rgba(59, 130, 246, 0.2),
                inset 0 0 10px rgba(59, 130, 246, 0.1);
        }

        .res-logo {
            width: 60px;
            height: 32px;
            margin: 0 auto 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 10px;
            border-radius: 4px;
            position: relative;
        }

        .res-logo.fhd {
            background: linear-gradient(135deg, #1e40af, #1e3a8a);
            color: white;
        }

        .res-logo.fhd::before {
            content: "FULL HD";
            position: absolute;
            top: 4px;
            font-size: 8px;
            font-weight: 600;
        }

        .res-logo.fhd::after {
            content: "1080p";
            position: absolute;
            bottom: 4px;
            font-size: 10px;
            font-weight: 700;
        }

        .res-logo.uhd {
            background: linear-gradient(135deg, #1e40af, #111827);
            color: white;
        }

        .res-logo.uhd::before {
            content: "4K";
            position: absolute;
            font-size: 16px;
            font-weight: 900;
            top: 2px;
        }

        .res-logo.uhd::after {
            content: "ULTRA HD";
            position: absolute;
            bottom: 2px;
            font-size: 6px;
            font-weight: 500;
            letter-spacing: 0.5px;
        }

        .res-text {
            color: #e0e0e0;
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .res-size {
            color: rgba(255, 255, 255, 0.6);
            font-size: 12px;
        }

        /* Action buttons */
        .actions {
            display: flex;
            gap: 16px;
            margin-top: auto;
            padding-top: 24px;
        }

        .btn {
            flex: 1;
            padding: 16px;
            border: none;
            border-radius: 12px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .btn-cancel {
            background: rgba(15, 15, 25, 0.8);
            color: rgba(255, 255, 255, 0.8);
            border: 2px solid rgba(255, 255, 255, 0.15);
        }

        .btn-cancel:hover {
            background: rgba(25, 25, 35, 0.9);
            border-color: rgba(255, 255, 255, 0.25);
            color: #ffffff;
        }

        .btn-save {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.9), rgba(37, 99, 235, 0.9));
            color: #ffffff;
            border: 2px solid rgba(59, 130, 246, 0.5);
        }

        .btn-save:hover {
            background: linear-gradient(135deg, rgba(59, 130, 246, 1), rgba(37, 99, 235, 1));
            box-shadow: 0 8px 24px rgba(59, 130, 246, 0.3);
            transform: translateY(-1px);
        }

        /* Hidden file input */
        input[type="file"] {
            display: none;
        }
    </style>
</head>
<body>
    <div class="background"></div>
    
    <div class="settings-window">
        <h2 class="settings-title">Settings</h2>
        
        <!-- Logo Upload Area -->
        <div class="logo-upload" onclick="document.getElementById('logoUpload').click()">
            <div class="logo-upload-text">Your Logo</div>
            <input type="file" id="logoUpload" accept="image/*">
        </div>
        
        <!-- Background Selection -->
        <div>
            <div class="section-title">Background</div>
            <div class="background-options">
                <div class="bg-option bg-white" data-bg="white"></div>
                <div class="bg-option bg-black selected" data-bg="black"></div>
                <div class="bg-option bg-custom" data-bg="custom">
                    <div class="bg-custom-text">Custom</div>
                </div>
            </div>
        </div>
        
        <!-- Resolution Selection -->
        <div>
            <div class="section-title">Resolution</div>
            <div class="resolution-options">
                <div class="res-option selected" data-res="fhd">
                    <div class="res-logo fhd"></div>
                    <div class="res-text">Full HD</div>
                    <div class="res-size">1920×1080</div>
                </div>
                <div class="res-option" data-res="4k">
                    <div class="res-logo uhd"></div>
                    <div class="res-text">4K Ultra HD</div>
                    <div class="res-size">3840×2160</div>
                </div>
            </div>
        </div>
        
        <!-- Action Buttons -->
        <div class="actions">
            <button class="btn btn-cancel">Cancel</button>
            <button class="btn btn-save">Save</button>
        </div>
    </div>

    <script>
        // Background selection
        const bgOptions = document.querySelectorAll('.bg-option');
        bgOptions.forEach(option => {
            option.addEventListener('click', () => {
                bgOptions.forEach(opt => opt.classList.remove('selected'));
                option.classList.add('selected');
            });
        });

        // Resolution selection
        const resOptions = document.querySelectorAll('.res-option');
        resOptions.forEach(option => {
            option.addEventListener('click', () => {
                resOptions.forEach(opt => opt.classList.remove('selected'));
                option.classList.add('selected');
            });
        });

        // Logo upload handling
        document.getElementById('logoUpload').addEventListener('change', function(e) {
            if (e.target.files.length > 0) {
                const fileName = e.target.files[0].name;
                document.querySelector('.logo-upload-text').textContent = fileName;
            }
        });

        // Button actions
        document.querySelector('.btn-cancel').addEventListener('click', () => {
            console.log('Cancel clicked');
            // Add your cancel logic here
        });

        document.querySelector('.btn-save').addEventListener('click', () => {
            const selectedBg = document.querySelector('.bg-option.selected').dataset.bg;
            const selectedRes = document.querySelector('.res-option.selected').dataset.res;
            console.log('Save clicked', { background: selectedBg, resolution: selectedRes });
            // Add your save logic here
        });
    </script>
</body>
</html>